.large-12.columns
  .row
    .large-6.columns
      %p
        = Settings.app_name
        (#{Rails.env})
        = Settings.app_version
        (#{link_to "Github","https://github.com/ut/zakk-admin"})
    .large-6.columns
      - if !current_user
        %p
          = link_to "Login",user_session_path
