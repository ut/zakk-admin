:css
  body, main { background-color: #{program.backgroundcolor}}
  main { background-image: url(#{program.backgroundimage.small_thumb.url}) }
  .panel h1,
  .panel h2,
  .panel h3,
  .panel h4 { color: #{program.textcolor2}}
  strong { color: #{program.textcolor1}}